# Project Instructions

## Git Branch Naming

When creating git branches, name them using the date format: `claude/YYYY-MM-DD` (e.g., `claude/2026-05-26`). If multiple branches are needed on the same day, append a suffix like `claude/2026-05-26-2`.
